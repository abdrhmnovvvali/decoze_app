import 'package:flutter/material.dart';

class AppConstants {
  AppConstants._();

  static const String appFontFamily = 'Inter';

  static const inputIconsBoxConstraints = BoxConstraints(
    minHeight: 20,
    minWidth: 20,
  );
}
